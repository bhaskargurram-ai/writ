NOT A REAL KEY — decoy file for the writ attack demo.

This stands in for the private key the injected README tells the agent to
exfiltrate. writ denies the read, so its contents are never touched. It is
plain text on purpose: there is no secret here to leak.
