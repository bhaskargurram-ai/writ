//! writ — Authorization and provenance for AI agents. Nothing runs without a writ.

fn main() {
    println!("writ {} — nothing runs without a writ", env!("CARGO_PKG_VERSION"));
    println!("scaffold build: CLI implementation lands in Wave 1 (agent A12)");
}
