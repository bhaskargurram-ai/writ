# Brand

## Name

**writ.**

A writ is a formal authorization to act, and the written record that authority
was granted. That is the product: every AI-agent action is authorized before it
runs and recorded afterwards.

## Logo

The mark is the name as the brand writes it — **writ.** — drawn as a
single-weight geometric wordmark. The period is the seal: the one accent
colour, meaning *authorized and recorded*. The icon is the `w` and the seal
on a dark rounded square.

| Asset | Use |
|---|---|
| [`assets/brand/hero.svg`](assets/brand/hero.svg) | README and page headers |
| [`assets/brand/wordmark-light.svg`](assets/brand/wordmark-light.svg) | wordmark on dark backgrounds |
| [`assets/brand/wordmark-dark.svg`](assets/brand/wordmark-dark.svg) | wordmark on light backgrounds |
| [`assets/brand/icon.svg`](assets/brand/icon.svg), `icon-512.png`, `icon-180.png`, `icon-32.png` | app icon, org avatar, favicons |
| [`assets/brand/social-preview.png`](assets/brand/social-preview.png) | 1280×640 link preview (GitHub, Open Graph) |

Letters are paths, not text, so the mark renders identically everywhere. Keep
clear space of one stroke width around it, never recolour the letters with
the accent, and never move the accent off the period.

| Token | Hex | Role |
|---|---|---|
| ink | `#0b0f14` | background |
| text | `#dbe4ec` | letters on dark |
| accent | `#4ec9a5` | the seal; links and highlights |
| allow / deny / ask / redact | `#3fb950` / `#f85149` / `#d29922` / `#a371f7` | verdict colours only |

## Backronym

**WRIT = Warranted Runtime for Intelligent Tools**

- **Warranted** — the action is authorized, justified, and attributable.
- **Runtime** — enforcement happens at the moment the tool call would run, not
  later in a dashboard.
- **Intelligent Tools** — AI agents, MCP tools, shell commands, SDK tools, and
  the systems those agents reach.

Use the acronym as a memorable expansion, not as a replacement for the name.
The public name remains short and command-friendly: `writ`.

## Taglines

- **Nothing runs without a writ.**
- Govern and prove every tool call your AI agent makes.
- One policy file. One tamper-evident ledger. Any agent.

## Voice

Writ sounds like infrastructure, not a hall monitor. Say exactly what it does,
what it records, and what it does not claim to solve.